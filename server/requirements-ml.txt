# Heavy ML dependencies, installed in the Docker image but NOT in CI —
# tests use a fake embedder so they never need model weights.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.12.0
open_clip_torch==3.3.0

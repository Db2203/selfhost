# server

FastAPI backend for PhotoNest: authentication, the asset API, and the background workers
that index photos, generate thumbnails, and compute AI embeddings.

Implementation starts in Phase 0 (see the project roadmap): `docker-compose` stack,
database schema + migrations, the storage interface, and authentication.

_Placeholder — code lands in the next stage._
